server {
    listen 80;
    server_name site1.local;

    location /protected {
        auth_request /_auth;
        proxy_pass http://backend;
    }

    location /_auth {
        internal;
        if ($http_x_api_key != "secret-key") {
            return 403;
        }
        return 200;
    }
}