server {
    listen 80;
    server_name site1.local;

    # Add Content Security Policy header for the entire server
    add_header Content-Security-Policy "script-src 'self'; style-src 'self'" always;
}
